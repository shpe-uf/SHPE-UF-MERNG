{
  "name": "merng-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "server": "node index",
    "client": "node scripts/start-client",
    "start": "concurrently \"nodemon index\" \"npm run client\"",
    "build": "node scripts/build",
    "seed": "node scripts/seedDB",
    "test": "echo \"Error: no test specified\" && exit 1",
    "installDeps": "yarn && cd client && yarn",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "apollo-server": "^2.9.0",
    "bcryptjs": "^2.4.3",
    "dotenv": "^8.1.0",
    "graphql": "^14.5.1",
    "jsonwebtoken": "^8.5.1",
    "mongo": "^0.1.0",
    "mongoose": "^5.6.10",
    "nodemon": "^1.19.2",
    "react-router-dom": "^5.0.1",
    "semantic-ui-css": "^2.4.1",
    "semantic-ui-react": "^0.88.0"
  }
}
